import React from 'react';

const SidebarBodySection = ({ children }) => {
    return (
        <div className='sidebar-body-section'>
            { children }
        </div>
    )
}

export default SidebarBodySection;